//Arrays con las reglas de encriptacion y desencriptacion
let letraOriginal = ['e', 'i', 'a', 'o', 'u'];
let letraEncriptada = ['enter', 'imes', 'ai', 'ober', 'ufat'];

//Funcion para encriptar el texto usando arrays
function encriparTexto(texto){
    let textoEncriptado = texto;
    for(let i = 0; i < letraOriginal.length; i++){
        let expresionRegular = new RegExp
    }
}